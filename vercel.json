{
  "version": 2,
  "builds": [
    {
      "src": "dist/server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "methods": ["OPTIONS", "GET", "POST"],
      "dest": "dist/server.js",
       "headers": {
        "Access-Control-Allow-Origin": "*"
      }
    }
  ]
}
